const ts = require('typescript'); const fs = require('fs'); const content = fs.readFileSync('moolam/pagudhigal/ProfileEditor.tsx', 'utf8'); const source = ts.createSourceFile('ProfileEditor.tsx', content, ts.ScriptTarget.Latest, true); const diagnostics = source.parseDiagnostics; console.log(diagnostics.map(d => { const pos = ts.getLineAndCharacterOfPosition(source, d.start); return 'Line ' + (pos.line + 1) + ': ' + d.messageText; }).join('\n'));
