package com.elvan.udukkai.core.constants

object AppConstants {
    const val APP_NAME = "Udukkai"
    const val APP_VERSION = "1.0.0"
    // Database names are defined in AppMode enum (one per mode)
    // See: core/mode/AppMode.kt
    const val PREFS_SELECTED_MODE = "udukkai_selected_mode"
}
