// ─────────────────────────────────────────────────────────────────────────────
// ANALYTICS STUB — Currently no-op
// ─────────────────────────────────────────────────────────────────────────────
// When Firebase Analytics is configured, uncomment the implementation.

// import 'package:firebase_analytics/firebase_analytics.dart';

/// Log a custom event. Currently a no-op.
Future<void> nigazhvuPathivu(String eventName, {Map<String, Object>? parameters}) async {
  // await FirebaseAnalytics.instance.logEvent(
  //   name: eventName,
  //   parameters: parameters,
  // );
}

/// Log a screen view. Currently a no-op.
Future<void> thiraiPathivu(String screenName) async {
  // await FirebaseAnalytics.instance.logScreenView(screenName: screenName);
}
