// ─────────────────────────────────────────────────────────────────────────────
// FIREBASE INITIALIZATION — Currently disabled (stub)
// ─────────────────────────────────────────────────────────────────────────────
// Uncomment the body when Firebase is configured via `flutterfire configure`.
// This file provides a single entry point for all Firebase service init.

// import 'package:firebase_core/firebase_core.dart';
// import 'package:firebase_crashlytics/firebase_crashlytics.dart';
// import 'package:firebase_analytics/firebase_analytics.dart';
// import 'firebase_vilaivu.dart';

/// Initialize all Firebase services.
/// Call this from main() BEFORE runApp().
Future<void> firebaseThuvakku() async {
  // ── Step 1: Initialize Firebase Core ──
  // await Firebase.initializeApp(
  //   options: DefaultFirebaseOptions.currentPlatform,
  // );

  // ── Step 2: Crashlytics ──
  // FlutterError.onError = FirebaseCrashlytics.instance.recordFlutterFatalError;

  // ── Step 3: Analytics ──
  // final analytics = FirebaseAnalytics.instance;
  // await analytics.setAnalyticsCollectionEnabled(true);
}
